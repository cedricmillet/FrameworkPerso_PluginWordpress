<?php

	if(!defined('ABSPATH')) exit('ACCES INTERDIT.');

	/**
	 * Classe enfant du plugin
	 */
	class Abyxo_Security_exemple extends Abyxo_Security
	{
		function __construct( $args = array() )
		{
			/*
			add_filter( 'login_redirect', array($this, 'login_redirect_page'), 10, 3 );
			add_action( 'admin_notices', array($this, 'ajouter_balise_user_is_adminlite') );
			add_action( 'wp_footer', array($this, 'ajouter_balise_user_is_adminlite') );
			*/
		}





		
	}

